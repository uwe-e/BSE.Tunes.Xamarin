<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BSE.Tunes.XApp.Views.Templates.SlidePanelTemplate"
             xmlns:ffimageLoading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms">
    <ContentView.Content>
        <Grid>

            <ffimageLoading:CachedImage Source="{Binding ImageSource}" Aspect="AspectFill"/>

            <StackLayout BackgroundColor="#33000000" VerticalOptions="End" HorizontalOptions="FillAndExpand">
                <Label HorizontalTextAlignment="Center"
                       Text="{Binding Title}"
                       TextColor="White"
                       LineBreakMode="TailTruncation"/>
                <Label HorizontalTextAlignment="Center" 
                       Margin="0,-5,0,0"
                       FontSize="13"
                       Text="{Binding SubTitle}"
                       TextColor="White"
                       LineBreakMode="TailTruncation"/>
            </StackLayout>
        </Grid>
  </ContentView.Content>
</ContentView>