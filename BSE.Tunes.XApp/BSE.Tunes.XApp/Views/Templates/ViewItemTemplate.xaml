<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ffimageLoading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             x:Class="BSE.Tunes.XApp.Views.Templates.ViewItemTemplate">
    <ContentView.Content>
        <StackLayout Margin="3,0,3,0" WidthRequest="160" HeightRequest="200">
            <Frame Padding="0"
                   CornerRadius="3"
                   IsClippedToBounds="True"
                   HasShadow="False">
                <ffimageLoading:CachedImage Source="{Binding ImageSource}"
                                        Aspect="AspectFill"
                                        WidthRequest="160" HeightRequest="160"/>
                <!--<Grid WidthRequest="160" HeightRequest="160" BackgroundColor="Red"/>-->
            </Frame>
            <Label Text="{Binding Title}"
                   Margin="0,-5,0,0"
                   TextColor="{DynamicResource PrimaryLabelColor}"
                   LineBreakMode="TailTruncation"/>
            <Label Text="{Binding SubTitle}"
                   Margin="0,-5,0,0"
                   FontSize="13"
                   TextColor="{DynamicResource SecondaryLabelColor}"
                   LineBreakMode="TailTruncation"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>